# Inventory Service (Standalone) — Java 22

## Run
```bash
mvn spring-boot:run
# then visit:
curl http://localhost:8080/api/info
```

Make sure the Config Server is running at `http://localhost:8888`.
